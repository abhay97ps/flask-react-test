services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    image: flask-api
    ports:
      - "8000:8000"
  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    image: react-client
    ports:
      - "3000:80"
